# filename|source_url
# Production import bridge. Add a temporary source only long enough for GitHub Actions to capture and permanently commit the asset under daa-savings/production/.
# Keep this manifest empty after successful import.
