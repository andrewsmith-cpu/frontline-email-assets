name: Render DAA Savings Batch — MANUAL ONLY

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Safety stop
        run: |
          echo "Automatic DAA Genie batch rendering is disabled."
          echo "Use the original verified DAA calculator / V8.7 data path only."
          exit 1
